\subsectionlang{en}{File Types}
\lang{en}{
	There are two fundamentally different classes of file formats:
	\begin{itemize}
		\item \textbf{vector} formats like \textbf{SVG, EPS, AI, PDF} (is often used as a container for vector formats), etc.
		\item \textbf{raster} (pixel) formats like \textbf{JP(E)G, PNG, WEBP, GIF, TIFF, BMP}, etc.
	\end{itemize}
}

\subsectionlang{de}{Dateitypen}
\lang{de}{
	Es gibt zwei grundlegend verschiedene Arten von Bilddateien:
	\begin{itemize}
		\item \textbf{Vektorgrafiken} wie z.B. \textbf{SVG, EPS, AI, PDF} (PDF wird of als Container für Vektorgrafiken verwendet), etc.
		\item \textbf{Rastergrafiken} (Pixelgrafiken) wie z.B. \textbf{JP(E)G, PNG, WEBP, GIF, TIFF, BMP}, etc.
	\end{itemize}
}
